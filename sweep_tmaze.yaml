# This file defines the sweep for n_mem_states = 2
program: scripts/batch_run_kitchen_sinks_single_obj.py
method: grid # bayes, random, grid (if grid then all parameters have to have a list of values, e.g. can't use "uniform" or "int_log_uniform" etc.)
metric:
  name: metric # has to be a key in the wandb.log
  goal: maximize # We want to maximize performance
parameters:
  # -- Fixed parameter for this sweep --
  n_mem_states:
    values: [2, 4]

  # -- Hyperparameters to search --
  num_gammas:
    value: 1 #[1, 3, 5] # Categorical search

  mi_steps:
    #distribution: int_log_uniform
    #min: 10000
    #max: 200000
    value: 100000 #[30000, 100000, 200000]
  
  pi_steps:
    value: 3000

  gamma_type:
    value: uniform
  gamma_min:
    #distribution: uniform
    #min: 0.3
    #max: 0.9
    value: 0.9 #[0.5, 0.75, 0.9, 0.95]
  gamma_max:
    value: 0.9
  
  lambda_0:
    values: [0.0, 0.2, 0.4]

  lambda_1:
    values: [0.6, 0.8, 1.0]
  
  # -- You can also fix other parameters if needed --
  n_seeds:
    value: 10
  platform:
    value: gpu
  seed:
    value: 42

  spec:
    value: parity_check
  policy_optim_alg:
    value: policy_grad
  value_type:
    value: q
  error_type:
    value: l2
  alpha:
    value: 1.0
  objective:
    value: ld
  optimizer:
    value: adam
  pi_lr:
    value: 0.01
  mi_lr:
    value: 0.01
  study_name:
    value: hyperparams
  reward_in_obs:
    value: true

# Add this entire command section
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_boolean_flags}
  - ${if:reward_in_obs, "--reward_in_obs"}